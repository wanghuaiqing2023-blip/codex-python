# codex-model-provider-info

Rust crate: `codex-model-provider-info`

Rust anchor: `codex/codex-rs/model-provider-info`

Current certified modules:

- `model-provider-info/src/lib.rs`

The Rust crate has one implementation module plus its Rust test file included
from `src/lib.rs`. Python maps the public provider registry contract in
`pycodex/model_provider_info/__init__.py`: provider dataclasses, wire API
validation, built-in provider catalog, OpenAI/Amazon Bedrock/OSS provider
construction, retry/timeout helpers, auth/AWS validation, TOML/mapping
deserialization, API-provider projection, environment-backed API keys, and
configured-provider merging.

Remaining Rust modules: none.
