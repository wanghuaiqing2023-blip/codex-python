# codex-experimental-api-macros src/lib.rs status

Rust source:

- `codex/codex-rs/codex-experimental-api-macros/src/lib.rs`

Python target:

- `pycodex/codex_experimental_api_macros/__init__.py`

Status: complete.

Implemented contract:

- Python exposes the behavior generated by Rust's
  `#[derive(ExperimentalApi)]` proc macro as runtime decorators and dataclass
  metadata.
- Enum derive behavior maps variants to optional experimental reason strings.
- Struct derive behavior checks direct experimental fields and nested
  experimental fields in declaration order.
- Experimental field registration mirrors Rust's `ExperimentalField` inventory
  payloads.
- Direct-field presence checks mirror the Rust helper branches for option,
  collection/map, bool, and always-present scalar types.
- Field serialized names use the Rust `snake_to_camel` helper behavior.

Validation:

- Focused validation:
  `python -m pytest tests/test_codex_experimental_api_macros_lib_rs.py -q`
  -> `8 passed`.
- Syntax validation:
  `python -m py_compile pycodex/codex_experimental_api_macros/__init__.py tests/test_codex_experimental_api_macros_lib_rs.py`
