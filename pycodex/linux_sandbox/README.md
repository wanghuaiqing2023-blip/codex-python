# pycodex.linux_sandbox

Canonical Python package for the Rust workspace crate:

- Rust crate: `codex/codex-rs/linux-sandbox`
- Python package: `pycodex/linux_sandbox`

The implementation is currently a Python command-construction shim used by the core shell/runtime path; it does not provide a native Linux sandbox executable.
