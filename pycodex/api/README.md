# pycodex.api

Rust crate: `codex-api`

Rust anchor: `codex/codex-rs/codex-api`

This package mirrors public exports from Rust `codex-api/src/lib.rs` for
`codex-core` runtime dependency compatibility.  Pure request/header/provider
helpers are ported; network endpoint clients are represented as explicit
interface shims.
