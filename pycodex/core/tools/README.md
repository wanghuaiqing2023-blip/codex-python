# `pycodex.core.tools`

Canonical Python coordinate for behavior aligned with Rust `codex-rs/core/src/tools`.

This package is for core-runtime tool behavior that belongs under Rust core's tools module tree, not for generic tool discovery surfaces or CLI-only helpers.
