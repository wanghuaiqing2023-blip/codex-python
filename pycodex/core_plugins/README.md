# pycodex.core_plugins

Rust crate: `codex-core-plugins`

Rust anchor: `codex/codex-rs/core-plugins`

This package mirrors the public crate interface exported from
`core-plugins/src/lib.rs`.  Marketplace constants and manager data shapes are
ported for dependency compatibility; marketplace install, cache, remote sync,
and plugin runtime behavior remain lightweight shims.
