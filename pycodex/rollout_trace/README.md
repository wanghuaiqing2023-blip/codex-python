# pycodex.rollout_trace

Rust crate: `codex-rollout-trace`
Rust path: `codex/codex-rs/rollout-trace`

This package carries Python projections of selected rollout trace writer and
hot-path helper contracts.

## Module Coverage

| Rust module | Python surface | Status | Notes |
|---|---|---|---|
| `src/lib.rs` | `pycodex.rollout_trace.__all__`, crate-root imports | complete_slice | Crate-root `pub use` facade coverage is explicit in Python `__all__`, including writer/context/raw-payload/model/replay public names from Rust `lib.rs` and `model::*`, while reducer implementation helpers stay private. |
| `src/model/mod.rs` | `pycodex.rollout_trace` public model facade | complete_slice | Public model aliases/types are exported through the crate-root facade and reduced `RolloutTrace` graph export omits Python reducer internals; crate-level promotion audit passed on 2026-06-22. |
| `src/bundle.rs`, `src/reducer/mod.rs` | bundle layout constants, manifest layout, fixed replay log path, unreduced `Other` event error | complete_slice | Standard manifest/log/payload/reduced-state file names, manifest default local layout, reducer replay's fixed `trace.jsonl` event-log path, reducer-wide raw payload ref bookkeeping, and Rust's explicit `RawTraceEventPayload::Other` no-reducer error are covered by Rust-derived tests. |
| `src/mcp.rs`, `src/thread.rs`, `src/reducer/tool.rs` | `McpCallTraceContext`, `MCP_CALL_ID_META_KEY`, MCP correlation replay | complete_slice | Disabled contexts preserve metadata unchanged. Enabled contexts add `codex_bridge_mcp_call_id` to object metadata, create object metadata from `None`, and leave non-object metadata unchanged. Thread MCP starts emit `McpToolCallCorrelationAssigned` with a trace-owned UUID, reducer replay attaches that UUID to the reduced `ToolCall`, and unknown/duplicate correlation guards are covered. |
| `src/payload.rs` | `RawPayloadKind`, `RawPayloadRef` | complete_slice | Rust serde shape for tagged payload kinds and bundle-local raw payload refs is covered by Rust-derived tests. |
| `src/raw_event.rs` | `RawTraceEventPayload`, `RawToolCallRequester`, `RawTraceEventContext` | complete_slice | Rust internally tagged event payload shape, writer envelope context, and `raw_payload_refs()` variant-specific reference extraction are covered by Rust-derived tests. |
| `src/writer.rs` | `TraceWriter` | complete_slice | Manifest writing, payload ordinal/id/path allocation, JSON payload files, event log append order, sequence numbering, and replayed rollout/thread/turn/inference/raw-payload state from the Rust writer test are covered. |
| `src/thread.rs` | `ThreadTraceContext`, `ThreadStartedTraceMetadata`, `AgentResultTracePayload` | complete_slice | Root bundle lifecycle, child-thread bundle reuse, disabled no-op behavior, lazy dispatch construction, explicit turn-start context, and delegated inference/compaction/code-cell context construction are covered. |
| `src/reducer/mod.rs`, `src/reducer/thread.rs` | `replay_bundle`, reduced `RolloutTrace` thread/turn model | complete_slice | Local bundle replay, raw-payload bookkeeping, rollout/thread/turn lifecycle reduction, spawned-thread metadata precedence, and duplicate/unknown/mismatched thread-turn guards are covered. |
| `src/inference.rs`, `src/reducer/inference.rs` | `InferenceTraceContext`, `InferenceTraceAttempt`, `trace_response_item_json`, reduced `InferenceCall` lifecycle | complete_slice | Propagation headers with UUID-shaped inference call ids, request/response payload recording, trace-specific reasoning-content serialization, terminal-event guard, inference replay, cancelled partial-response item reduction, turn-end cleanup, and late partial payload retention are covered. |
| `src/reducer/conversation.rs`, `src/reducer/conversation/normalize.rs`, `src/reducer/tool.rs` | `ConversationItem`, message/function-call/reasoning/compaction normalization, inference request/response conversation replay, model-visible tool-call linking | complete_slice | Full request snapshots reuse prior matching history without deduping new identical items, may reorder existing items by content while inserting fresh summary items, response message/function-call/reasoning/custom-tool/hosted-tool outputs append to thread conversation items, JSON body identity ignores payload-local refs, incremental `previous_response_id` requests rebuild prior request+response prefixes, custom tool calls normalize exec input as JavaScript code and other string input as text, hosted/search/image/local-shell calls and search/MCP outputs normalize as JSON-backed function-call items, reasoning encrypted-content identity reuses response items while preserving readable evidence, complementary text/summary sightings merge, conflicting readable text keeps first evidence, token usage is reduced, compaction checkpoints insert marker and fresh replacement items, response/replacement items record producer refs, runtime tool calls link response `function_call` items plus later `function_call_output` items, and reducer errors are raised for reused model-visible call ids with different content, missing/unsupported model item type boundaries, missing/unsupported message roles, malformed reasoning parts, missing request input, and unknown `previous_response_id`. |
| `src/model/conversation.rs`, `src/model/runtime.rs`, `src/model/session.rs`, `src/model/mod.rs` | `RolloutStatus`, `ExecutionStatus`, `ConversationRole`, `ConversationChannel`, `ConversationItemKind`, `CodeCellRuntimeStatus`, `TerminalOperationKind`, `TerminalObservationSource`, `ConversationPart`, `ProducerRef`, `AgentOrigin`, `ToolCallRequester`, `ToolCallKind`, `ToolCallSummary`, `InteractionEdgeKind`, `TerminalRequest`, `TraceAnchor`, `AgentThread`, `CodexTurn`, `ConversationItem`, `InferenceCall`, `TokenUsage`, `CodeCell`, `ToolCall`, `TerminalSession`, `TerminalOperation`, `TerminalResult`, `TerminalModelObservation`, `InteractionEdge`, `CompactionRequest`, `Compaction`, `RolloutTrace` | complete_slice | Plain model enums serialize as Rust snake_case values. Tagged model enum variants are projected with Rust internally tagged snake_case JSON shape, omit fields unrelated to the active variant, and retain active-variant `Option` fields as JSON `null` where Rust's derived serde does. Tool dispatch/replay now normalizes requester/kind/summary into the Rust runtime model shapes, agent interaction edges use the Rust edge-kind enum values, session/conversation/runtime/compaction model structs project Rust public field names including nullable Option fields, and reduced `RolloutTrace` graph export includes only Rust public model fields. |
| `src/compaction.rs`, `src/reducer/compaction.rs`, `src/reducer/conversation.rs` | `CompactionTraceContext`, `CompactionTraceAttempt`, reduced `CompactionRequest` and installed `Compaction` lifecycle | complete_slice | Disabled no-op, request/response/failure payload recording, request replay, completion guards, checkpoint install request-id association, duplicate install guards, turn/thread ownership guards, malformed checkpoint payload guards, and full checkpoint conversation replacement are covered. |
| `src/code_cell.rs`, `src/reducer/code_cell.rs`, `src/reducer/tool.rs` | `CodeCellTraceContext`, reduced `CodeCell` lifecycle, queued runtime events, nested/wait tool links, source/output conversation links | complete_slice | Writer-side code-cell contexts record started/initial/ended lifecycle events with thread/turn context, map runtime responses to `CodeCellRuntimeStatus`, and write `ToolResult` raw payloads as `{"response": ...}`. Code cell starts can queue until their model-visible `custom_tool_call` source item is reduced; queued initial/end lifecycle events replay after source materialization; runtime status maps to execution status; cancelled/failed/aborted owning turns terminate running cells; custom-tool outputs receive `ProducerRef.CodeCell`; nested `RawToolCallRequester::CodeCell` tool calls are reduced through thread-local runtime cell ids; same-thread runtime cell id conflicts are rejected while cross-thread reuse is allowed; model-visible `wait` tool invocation payloads link back to cells by `cell_id`. |
| `src/reducer/tool/terminal.rs`, `src/model/runtime.rs` | reduced `TerminalOperation`, `TerminalSession`, terminal result/request/observation projection | complete_slice | Protocol runtime begin/end payloads create terminal operations and sessions for `exec_command`/`write_stdin`; direct dispatch-only `write_stdin` creates terminal rows from invocation/result payloads; code-mode `write_stdin` structured results preserve exit code, output, token count, and chunk id; terminal sessions are reused by process id; model-visible call/output items sync into direct terminal observations. |
| `src/reducer/tool/agents.rs` | reduced `InteractionEdge`, `TraceAnchor`, agent interaction edge projection | complete_slice | Spawned child-thread metadata creates `AgentOrigin::Spawned` with a stable spawn edge id without materializing a delivery edge; spawn-agent runtime payloads prefer the child-side delivered task message and fall back to the child thread when no delivery item exists; send-message payloads target the recipient-side mailbox item; close-agent payloads target the child thread; agent-result notifications link child result messages or child-thread fallbacks to the parent notification item while preserving raw payload evidence. |
| `src/tool_dispatch.rs`, `src/thread.rs` | `ToolDispatchTraceContext`, `ToolDispatchInvocation`, `ToolDispatchPayload`, `ToolDispatchRequester`, `ToolDispatchResult` | complete_slice | Disabled thread contexts keep lazy dispatch construction unevaluated; public code-mode `exec` custom dispatch is suppressed only when unnamespaced; enabled dispatch writes Rust-shaped `ToolInvocation` and `ToolResult` payloads, `ToolCallStarted` / `ToolCallEnded` events, model vs code-cell requester fields, generic summaries, Rust tool-name kind aliases, namespace-qualified labels, Rust `truncate_preview` char-boundary input previews, `Function`/`ToolSearch`/`Custom`/`LocalShell` invocation payload JSON shapes, direct/code-mode completed-result payloads, and failed-result payloads. |
| `src/protocol_event.rs`, `src/thread.rs` | `record_protocol_event`, `record_codex_turn_event`, `record_tool_call_event` | complete_slice | The Rust `wrapped_protocol_event_type` allowlist is covered for session/turn/thread rollback/error/warning/shutdown breadcrumbs while unselected protocol traffic is filtered; selected protocol events are wrapped as `ProtocolEventObserved` breadcrumbs with raw protocol payloads. Protocol turn lifecycle events map to typed Codex turn start/end records including abort fallback status; exec/patch/MCP/collab runtime protocol events map to tool runtime start/end records, with user-shell exec filtering and Rust status mapping. |

## Known Gaps

- No known Rust module/test parity gaps remain for the dependency-light Python
  projection. The crate is marked `complete` after the 2026-06-22 focused
  validation passed with `100 passed`, plus the external facade smoke passed.

## Tests

- `tests/test_rollout_trace_mcp_rs.py`
- `tests/test_rollout_trace_raw_event_writer_rs.py`
- `tests/test_rollout_trace_thread_rs.py`
- `tests/test_rollout_trace_reducer_thread_rs.py`
- `tests/test_rollout_trace_inference_rs.py`
- `tests/test_rollout_trace_compaction_rs.py`
- `tests/test_rollout_trace_conversation_rs.py`
- `tests/test_rollout_trace_code_cell_rs.py`
- `tests/test_rollout_trace_terminal_rs.py`
- `tests/test_rollout_trace_agents_rs.py`
- `tests/test_rollout_trace_model_rs.py`
- `tests/test_rollout_trace_tool_dispatch_rs.py`
- `tests/test_rollout_trace_protocol_event_rs.py`
- `tests/test_rollout_trace_lib_rs.py`
- Existing facade smoke: `tests/test_external_crate_interfaces.py -k rollout_trace`
