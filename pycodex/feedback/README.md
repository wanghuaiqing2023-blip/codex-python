# pycodex.feedback

Rust crate: `codex-feedback`

Rust anchor: `codex/codex-rs/feedback`

This package mirrors the public interface from `feedback/src/lib.rs`.  Local
feedback tag and log-buffer shapes are ported; remote feedback/Sentry upload is
an explicit unported boundary.
