# pycodex.memories_read

Rust crate: `codex-memories-read`

Rust anchor: `codex/codex-rs/memories/read`

This package mirrors the public crate interface exported from
`memories/read/src/lib.rs`.  The current interface ports the source-confirmed
`memory_root` path helper used by `codex-core`.
